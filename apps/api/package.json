{
  "name": "@agentos/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd ../.. && PYTHONPATH=$(pwd) apps/api/.venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir apps/api"
  }
}
