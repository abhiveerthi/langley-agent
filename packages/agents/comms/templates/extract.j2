You are extracting structured fields from an email draft.

Given the draft below, return a JSON object with these fields exactly:
{
  "subject": "...",
  "body": "...",
  "recipient": "..."
}

If the recipient is not specified anywhere in context, use
"{{ brand.primary_email }}" as a placeholder.

Return ONLY valid JSON, no markdown.
